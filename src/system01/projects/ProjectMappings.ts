import { LayerId } from '../types'

export const PROJECT_LAYER_MAP: Record<string, LayerId> = {
  'prerna': 'INTERFACES',
  'omniscient': 'REASONING',
  'mnemosyne': 'RETRIEVAL',
  'leadguard': 'DATA',
  'netflix_recsys': 'MODELS',
  'prometheus': 'EVALUATION'
}
